{
  "template": {
    "query": {
      "query_string": {
        "query": "{{query_string}}"
      }
    },
    "sort": [
      {
        "{{sort_field}}": {
          "order": "{{sort_order}}"
        }
      }
    ],
    "size":"{{results_per_page}}",
    "from":"{{from}}"
  }
}
